{
  "$schema": "http://json-schema.org/schema",
  "$id": "0",
  "title": "ng new options schema",
  "type": "object",
  "description": "Generates a new basic application setup with ngrx store for your graphQL schema ",
  "properties": {
    "name": {
      "description": "Name of the new app.",
      "type": "string",
      "pattern": "^(?:@[a-zA-Z0-9-*~][a-zA-Z0-9-*._~]*/)?[a-zA-Z0-9-~][a-zA-Z0-9-._~]*$",
      "default": "default-app",
      "x-prompt": "What name would you like to use for the application?"
    },
    "gqlFilePath": {
      "type": "string",
      "description": "Path of the graphQl schema",
      "minLength": 1,
      "default":"./graphql-schemas/astronauts.graphql",
      "x-prompt": "Please fill the name of your graphQL schema (put schema file in : /graphql-schemas/<your-schema-name>.graphql)"
    }
  },
  "required": ["name","gqlFilePath"]
 }